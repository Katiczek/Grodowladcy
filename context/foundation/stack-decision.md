---
status: manual-outside-registry
engine: Unity
platform_mvp: Android
created: 2026-09-23
bootstrapper: skip
---

# Stack decision — Grodowładcy

Unity, created manually (e.g. Unity Hub), Android as MVP target.

This is **not** a `/10x-bootstrapper` hand-off. The course starter registry has no Unity card; there is no valid `starter_id` to write into `tech-stack.md`.

Product requirements remain in `context/foundation/prd.md`.

## Next steps (human)

1. Create a Unity project (2D or 3D URP — your call; hex strategy usually fits 2D or orthographic 3D).
2. Enable Android build support in Unity.
3. Implement against the PRD Primary flow (solo turn loop, autosave, hero accept/resolve rules).
4. Skip `/10x-bootstrapper` unless you later switch to a registry starter (Expo/Flutter).
